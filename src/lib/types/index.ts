export interface IAngle {
  id: number;
  math: string;
};

export interface IPoint {
  id: number;
  name: string;
  d: string;
};

export interface INav {
  id: number;
  name: string;
  link: string;
  active: number;
};
